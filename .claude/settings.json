{
  "$comment": "Parent folder settings — Telegram plugin DISABLED here so you can open this folder in VS Code without spawning a duplicate listener. The TelegramConfig/ subfolder is the one with plugin enabled — see TelegramConfig/.claude/settings.json.",
  "enabledPlugins": {
    "telegram@claude-plugins-official": false
  }
}
